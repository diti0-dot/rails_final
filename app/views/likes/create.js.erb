document.getElementById('like-section-<%= @post.id %>').innerHTML = 
  '<%= j button_to "Unlike (#{@post.likes.count})", 
        post_like_path(post_id: @post.id, id: @post.likes.find_by(user: current_user).id), 
        method: :delete,
        remote: true,
        class: "like-button" %>';